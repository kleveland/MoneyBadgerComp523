doctype html
html
  head
    title= title
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no")
    link(rel='stylesheet', href='/static/style/main.css')
    link(rel="stylesheet" href="/static/style/bootstrap.min.css")
    script(src="/static/js/jquery-3.2.1.slim.min.js" crossorigin="anonymous")
    script(src="/static/js/popper.min.js" crossorigin="anonymous")
    script(src="/static/js/bootstrap.min.js" crossorigin="anonymous")
    script(src="/static/js/main.js" crossorigin="anonymous")

  body
    nav.navbar.navbar-expand-lg.navbar-dark.bg-primary
        a(href="#").navbar-brand Quiz App
        button(type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation").navbar-toggler
            span.navbar-toggler-icon
        div.collapse.navbar-collapse#navbarSupportedContent
            ul.navbar-nav.mr-auto
                li.nav-item
                    a(href="/").nav-link Home
                li.nav-item
                    a(href="/grades").nav-link Grades
            ul.navbar-nav
                li.nav-item
                    span.navbar-text.onyen=user.onyen
                if(user.group_id == 1 || user.group_id == 2)
                    li.nav-item
                        a( href="/admin" role="button").navbar-btn.btn.btn-success.nav-but  Instructor Panel
                li.nav-item
                    a(href="/logout" role="button").navbar-btn.btn.btn-danger  Log Out
    block content
